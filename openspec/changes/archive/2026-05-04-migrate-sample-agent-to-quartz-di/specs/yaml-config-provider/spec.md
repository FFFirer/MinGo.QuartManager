## ADDED Requirements

_(None — this change does not introduce new YAML config capabilities.)_

## MODIFIED Requirements

_(None — Quartz configuration is moving from `NameValueCollection` to fluent API, but the YAML config provider behavior for Agent-level settings (`agent:`, `platform:`) remains unchanged.)_

## REMOVED Requirements

_(None.)_
