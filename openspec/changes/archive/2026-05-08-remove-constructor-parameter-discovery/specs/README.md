## No Capability Changes

This change has no new capabilities and no modified capabilities. It is a code health cleanup with zero behavioral impact:

- Removes dead commented-out code
- Adds an explicit-only discovery guard comment

No spec-level requirements are affected.
