# Project Baseline Behaviors

本文档定义 MinGo.QuartManager 项目的基准开发和交付行为。所有变更（Change）必须遵守。

---

## 1. 变更完成后必须输出 E2E 测试用例文档

### 1.1 规则

每个 OpenSpec Change 在实现完成后（或归档时），必须将本次变更涉及的全部 E2E 测试场景整理为一份独立的 `docs/e2e/<change-name>.md` 文件。

### 1.2 模板

```markdown
# E2E: <Change 标题>

**关联 Change**: `<change-name>`
**归档日期**: `YYYY-MM-DD`

---

## 场景列表

### <功能模块 A>

| # | 场景 | 前置条件 | 操作步骤 | 预期结果 | 验证状态 |
|---|------|---------|---------|---------|---------|
| 1 | 场景简述 | 需要什么状态 | 1. step 1<br>2. step 2 | 期望的行为 | ⏳ |

### <功能模块 B>

...
```

### 1.3 存储位置

`docs/e2e/<归档日期>-<change-name>.md`

例如：`docs/e2e/2026-05-01-agent-scheduler-platform.md`

### 1.4 验收标准

- 每个独立的功能点至少有一个正向场景
- 每个边界条件至少有一个异常/边缘场景
- 场景步骤必须可重复执行
- 包含前置条件和预期结果

---

## 2. E2E 场景分类必含项

每个 Change 的 E2E 文档必须覆盖以下维度（如适用）：

| 维度 | 说明 | 最低场景数 |
|------|------|-----------|
| **正向流程** | 核心功能正常执行 | ≥1 |
| **异常流程** | 参数错误、依赖缺失、权限不足 | ≥1 |
| **边界条件** | 空值、超长值、并发操作 | ≥1 |
| **数据持久化** | 重启后状态恢复 | ≥1 |
| **兼容性** | 旧端点/旧数据兼容 | 如有变更则必含 |

---

## 3. 文档维护

- 新 Change 完成后追加新的 E2E 文档，不修改已有文档
- 跨 Change 的回归测试场景由后续 Change 补充
- 每次归档 Change 时检查是否已生成对应 E2E 文档，缺失则阻塞归档

---

## 4. 违反处理

Pull Request / Change 归档时缺少 E2E 文档 → 需补充后方可归档。
